{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "prestart": "ARCHIVE_REPO_URL=\"https://github.com/YOUR_USERNAME/tekst-tv-archive.git\" && ARCHIVE_DIR=\"/tmp/tekst-tv-archive\" && rm -rf $ARCHIVE_DIR && git clone $ARCHIVE_REPO_URL $ARCHIVE_DIR && node ../scripts/generate-archive-index.js $ARCHIVE_DIR && rm -rf $ARCHIVE_DIR",
    "start": "react-scripts start",
    "prebuild": "ARCHIVE_REPO_URL=\"https://github.com/YOUR_USERNAME/tekst-tv-archive.git\" && ARCHIVE_DIR=\"/tmp/tekst-tv-archive\" && rm -rf $ARCHIVE_DIR && git clone $ARCHIVE_REPO_URL $ARCHIVE_DIR && node ../scripts/generate-archive-index.js $ARCHIVE_DIR && rm -rf $ARCHIVE_DIR",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
